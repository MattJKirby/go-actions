package uid

import (
	"testing"
)

func TestReference(t *testing.T) {
	// source := NewResourceUid(WithSubResource("source"))
	// target := NewResourceUid(WithSubResource("target"))

	// ref := NewReference(source, target)
	
	// expectedSource := &PartialReference{
	// 	ReferenceUid: ref.Uid,
	// 	ResourceUid: source,
	// }

	// expectedTarget := &PartialReference{
	// 	ReferenceUid: ref.Uid,
	// 	ResourceUid: target,
	// }

	// assert.Equals(t, expectedSource, ref.SourceReference())
	// assert.Equals(t, expectedTarget, ref.TargetReference())
}