package execution

import (
	"testing"
)

func TestResolveActionFromInstance(t *testing.T) {
	// flowApp := app.NewApp("testApp")
	// reg := testActions.GenerateActionValidEmptyRegistration()
	// app.RegisterAction(reg)

	// expected := app.GetAction[testActions.ActionValid, testActions.ActionValidProps](&testActions.ActionValidProps{})

	// resolver := NewActionResovler(app)
}
